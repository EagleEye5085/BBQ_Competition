<%= link_to "teams", "/teams" %>
<%= link_to "submissions", "/submissions" %>
<%= link_to "new team", "/teams/new" %>

<h1>Teams</h1>
<% @teams.each_with_index do |team, index| %>
<%= button_to "Edit #{team.name}", "/teams/#{team.id}/edit", method: :get %>
<%= link_to "Delete #{team.name}", "/teams/#{team.id}", method: :delete %>
  <div id="team-<%= index %>">
  <h3>Team name: <%= link_to "#{team.name}", "/teams/#{team.id}" %></h3>
  <p>Team created_at: <%= team.created_at %></p>
  </div>
<% end %>
